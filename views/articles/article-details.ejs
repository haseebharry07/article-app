
<main class="container article-details-page">

  <section class="article-detail">
    <% if (article.image) { %>
      <img src="/uploads/<%= article.image %>" alt="<%= article.title %>" class="article-detail-image">
    <% } %>

    <h1 class="article-title"><%= article.title %></h1>
    <p class="meta">Published on: <%= new Date(article.createdAt).toLocaleString() %></p>

    <p class="article-description">
      <%= article.description || article.content %>
    </p>

    <% if (article.websiteURL) { %>
      <p class="article-website">
        Product/Website: <a href="<%= article.websiteURL %>" target="_blank"><%= article.websiteURL %></a>
      </p>
    <% } %>
  </section>

</main>